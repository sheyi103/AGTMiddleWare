package db

import (
	"testing"
)

func TestCreateUser(t *testing.T) {
	// arg := CreateUserParams{
	// 	Name:          "Seyi",
	// 	Email:         "seyiolawepo@gmail.com",
	// 	PhoneNumber:   "2348026425250",
	// 	ContactPerson: " 50B  kano street Ebute metta",
	// 	RoleID:        2,
	// }

	// user, err := testQueries.CreateUser(context.Background(), arg)
	// require.NoError(t, err)
	// require.NotEmpty(t, user)
	// require.Equal(t, arg.Name, user.)
	// require.Equal(t, arg.Email, user.Email)
	// require.Equal(t, arg.PhoneNumber, user.PhoneNumber)
	// require.Equal(t, arg.ContactPerson, user.ContactPerson)
	// require.Equal(t, arg.RoleID, user.RoleID)

	// // require.Equal(t, sql.Result.LastInsertId(ID))

	// require.NotZero(t, user.ID)
	// require.NotZero(t, user.CreatedAt)
}
