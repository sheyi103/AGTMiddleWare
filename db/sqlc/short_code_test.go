package db

import (
	"testing"
)

func TestCreateShortCode(t *testing.T) {
	// name := "Seyi"

	// role, err := testQueries.CreateRole(context.Background(), name)
	// log.Fatal("no error", role)
	// require.NoError(t, err)
	// require.NotEmpty(t, role)

	// require.Equal(t, sql.Result.LastInsertId(ID))

	// require.NotZero(t, role.LastInsertId())
	// require.NotZero(t, role.CreatedAt)
}
